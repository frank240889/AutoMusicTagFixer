<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="CustomTrackView">
        <attr name="absoluteTrackPath" format="string" />
        <attr name="relativeTrackPath" format="string" />
        <attr name="trackName" format="string" />
        <attr name="albumName" format="string" />
        <attr name="authorName" format="string" />
        <attr name="titleName" format="string" />
        <attr name="bitrate" format="string" />
        <attr name="genre" format="string" />
        <attr name="duration" format="string" />
        <attr name="absolutePosition" format="string" />
        <attr name="isSelected" format="boolean" />
        <attr name="isPlaying" format="boolean" />
    </declare-styleable>

</resources>
